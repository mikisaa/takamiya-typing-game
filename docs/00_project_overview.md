# プロジェクト概要 (00_project_overview.md)

## Project Brief

# プロジェクト概要 (00_project_overview.md)

## Project Brief

### 1. プロジェクトの目的と概要
部署内で遊ぶことを主目的とした、**仮設足場・積込作業をテーマにした2D横スクロール風タイピングゲーム**（Project: `Base Typing Game` / slug: `base-typing-game`）を開発する。
教育アプリ然とさせるのではなく、ゲームとして普通に遊べる・熱中できることを最優先とする。
副次的な効果として以下を目指す：
* タイピング速度向上
* キーボード操作への習熟
* ブラインドタッチ練習

入力問題には会社・業務・仮設足場等に関係する言葉・用語を使用し、実際の問題文・単語データは後からユーザーが容易に作成・追加・管理できるデータ駆動構造とする。

### 2. 対象アーキテクチャ & 責務境界
* **Frontend**: HTML5, Vanilla CSS, JavaScript (ES6+), 2D Pixel Art / Pixel-style SVG, Browser-side local game engine
* **Backend**: Google Apps Script (GAS) Web App (JSON endpoint)
* **Data Store**: Google Spreadsheet (Players, Scores, Questions)
* **非使用技術**: Vercel, Firebase, Supabase, 社内ファイル共有DB, Google Workspace組織認証前提機能, 常時サーバー通信
* **通信原則**: ゲーム進行（タイピング判定、タイマー、COMBO、アニメーション、背景進化）はすべてブラウザ側JavaScriptで完結。GAS通信は「初期設定・Player master取得」「本番結果登録」「ランキング取得」に限定。1キー入力ごとの通信は厳禁。

### 3. ゲームモード & 難易度体系
3段階の難易度（初級・中級・上級）× 2つのプレイモード（練習モード・本番モード）を提供。
* **初級（軽トラック）**: フォークリフト走行速度が遅め。親しみやすい軽トラPixel Art。
* **中級（4tユニック）**: フォークリフト走行速度が標準。ユニッククレーン付き中型平ボディ。
* **上級（15tユニック）**: フォークリフト走行速度が速め。大型荷台・大型クレーン。
* **練習モード**: ランキング登録なし、タイピング練習特化、入力結果・正確率確認可能、演出は本番同様。
* **本番モード**: 制限時間制、SCORE・COMBO・TIME BONUS・MISS Penaltyあり、リザルト画面表示、Google Spreadsheetへのランキング登録。

### 4. ゲームプレイ & アニメーション演出
* **基本ループ**: 画面左から仮設足場資材（支柱、手摺、建枠、布板、筋交、ジャッキベース、小物資材パレット等）を爪に載せたフォークリフトが右へ走行。右側に停車中のトラックに到達する前に問題を正しくタイピングする。
* **成功演出（SUCCESS）**: トラック前で停止 → 爪/荷物が上昇 → 荷台へ移動 → 短いSUCCESS演出 → SCORE/COMBO更新 → 背景建設進行 → 次の問題へ移行（テンポ重視）。
* **失敗演出（MISS）**: トラックへ軽く接触 → 車体・画面が軽く揺れる → 爪上の資材が落下 → MISS表示 → COMBOリセット → 残り時間Penalty → 次の問題へ（コミカルな演出、負傷等なし）。
* **背景進化**: 更地 → コンテナ → 家 → ビル → 高層ビル → 東京タワー → スカイツリー（通常ステージ完成）へと正解数に応じて段階的に成長。
* **EXTRAステージ**: スカイツリー完成後の継続モード。完成都市上空・周辺にAirplane（旅客機通過）、Helicopter（ヘリ旋回）、Balloons（風船浮上）、Skydiving（パラシュート降下）、Rainbow（虹フェードイン）が動的に発生。

### 5. タイピングUX & ローマ字入力エンジン
* 日本語問題、ローマ字ターゲット、入力済み文字、未入力文字、ミスタイプ視覚フィードバックを明瞭に区分。
* 入力欄へのフォーカス操作不要（直接Keydown捕捉）。
* ローマ字入力ゆれ許容（し: shi/si, ち: chi/ti, つ: tsu/tu, ふ: fu/hu, じ: ji/zi, じゅ: ju/zyu/jyu, ん: n/nn 等）の柔軟な判定。

### 6. ランキング & データモデル
* ランキング期間: 「今月」「歴代」の2種類（今日・今週は不要）。
* 難易度別集計（初級・中級・上級）。本番モードのみ対象。
* 期間×難易度ごとに各プレイヤーの自己最高スコアを表示（同一人物によるランキング独占を防止）。
* Google Workspace組織環境がないため、Spreadsheet上のPlayer masterから名前を選択する軽量ID方式を採用。

### 7. セキュリティ & 信頼性
* GAS側パラメータ検証（難易度、スコア整合性、文字数等）。
* Spreadsheet formula injection対策（先頭 `=`, `+`, `-`, `@` のエスケープ）。
* GAS LockServiceによる同時書き込み排他制御。

---

## 1. プロジェクトの目的
部署内で楽しくプレイしながら自然とタイピングスキル（速度向上、ブラインドタッチ習熟、社内用語習熟）が身につく、仮設足場・積込2DタイピングWebアプリケーションの提供。

## 2. ターゲットユーザー
* 部署内の一般社員・作業スタッフ（PC操作初心者〜中上級者）
* タイピング練習を行いたいスタッフ、社内ランキングを競いたいスタッフ

## 3. ゴールと成功の定義
1. ブラウザ上で軽快に動作し、フォークリフト積込×背景発展演出による爽快なゲーム体験が得られること。
2. 日本語ローマ字入力判定が自然で、ミスなくスムーズにタイピングできること。
3. 練習モードおよび本番モードが正しく機能し、本番スコアがGoogle Spreadsheetへ記録され、月間・歴代ランキングが表示できること。
4. 単語データやゲーム設定（速度、ボーナス、ペナルティ等）が外部定義・設定可能であり、容易に拡張・調整できること。

### 1. プロジェクトの目的と概要
部署内で遊ぶことを主目的とした、**仮設足場・積込作業をテーマにした2D横スクロール風タイピングゲーム**（Project: `Base Typing Game` / slug: `base-typing-game`）を開発する。
教育アプリ然とさせるのではなく、ゲームとして普通に遊べる・熱中できることを最優先とする。
副次的な効果として以下を目指す：
* タイピング速度向上
* キーボード操作への習熟
* ブラインドタッチ練習

入力問題には会社・業務・仮設足場等に関係する言葉・用語を使用し、実際の問題文・単語データは後からユーザーが容易に作成・追加・管理できるデータ駆動構造とする。

### 2. 対象アーキテクチャ & 責務境界
* **Frontend**: HTML5, Vanilla CSS, JavaScript (ES6+), 2D Pixel Art / Pixel-style SVG, Browser-side local game engine
* **Backend**: Google Apps Script (GAS) Web App (JSON endpoint)
* **Data Store**: Google Spreadsheet (Players, Scores, Questions)
* **非使用技術**: Vercel, Firebase, Supabase, 社内ファイル共有DB, Google Workspace組織認証前提機能, 常時サーバー通信
* **通信原則**: ゲーム進行（タイピング判定、タイマー、COMBO、アニメーション、背景進化）はすべてブラウザ側JavaScriptで完結。GAS通信は「初期設定・Player master取得」「本番結果登録」「ランキング取得」に限定。1キー入力ごとの通信は厳禁。

### 3. ゲームモード & 難易度体系
3段階の難易度（初級・中級・上級）× 2つのプレイモード（練習モード・本番モード）を提供。
* **初級（軽トラック）**: フォークリフト走行速度が遅め。親しみやすい軽トラPixel Art。
* **中級（4tユニック）**: フォークリフト走行速度が標準。ユニッククレーン付き中型平ボディ。
* **上級（15tユニック）**: フォークリフト走行速度が速め。大型荷台・大型クレーン。
* **練習モード**: ランキング登録なし、タイピング練習特化、入力結果・正確率確認可能、演出は本番同様。
* **本番モード**: 制限時間制、SCORE・COMBO・TIME BONUS・MISS Penaltyあり、リザルト画面表示、Google Spreadsheetへのランキング登録。

### 4. ゲームプレイ & アニメーション演出
* **基本ループ**: 画面左から仮設足場資材（支柱、手摺、建枠、布板、筋交、ジャッキベース、小物資材パレット等）を爪に載せたフォークリフトが右へ走行。右側に停車中のトラックに到達する前に問題を正しくタイピングする。
* **成功演出（SUCCESS）**: トラック前で停止 → 爪/荷物が上昇 → 荷台へ移動 → 短いSUCCESS演出 → SCORE/COMBO更新 → 背景建設進行 → 次の問題へ移行（テンポ重視）。
* **失敗演出（MISS）**: トラックへ軽く接触 → 車体・画面が軽く揺れる → 爪上の資材が落下 → MISS表示 → COMBOリセット → 残り時間Penalty → 次の問題へ（コミカルな演出、負傷等なし）。
* **背景進化**: 更地 → コンテナ → 家 → ビル → 高層ビル → 東京タワー → スカイツリー（通常ステージ完成）へと正解数に応じて段階的に成長。
* **EXTRAステージ**: スカイツリー完成後の継続モード。完成都市上空・周辺にAirplane（旅客機通過）、Helicopter（ヘリ旋回）、Balloons（風船浮上）、Skydiving（パラシュート降下）、Rainbow（虹フェードイン）が動的に発生。

### 5. タイピングUX & ローマ字入力エンジン
* 日本語問題、ローマ字ターゲット、入力済み文字、未入力文字、ミスタイプ視覚フィードバックを明瞭に区分。
* 入力欄へのフォーカス操作不要（直接Keydown捕捉）。
* ローマ字入力ゆれ許容（し: shi/si, ち: chi/ti, つ: tsu/tu, ふ: fu/hu, じ: ji/zi, じゅ: ju/zyu/jyu, ん: n/nn 等）の柔軟な判定。

### 6. ランキング & データモデル
* ランキング期間: 「今月」「歴代」の2種類（今日・今週は不要）。
* 難易度別集計（初級・中級・上級）。本番モードのみ対象。
* 期間×難易度ごとに各プレイヤーの自己最高スコアを表示（同一人物によるランキング独占を防止）。
* Google Workspace組織環境がないため、Spreadsheet上のPlayer masterから名前を選択する軽量ID方式を採用。

### 7. セキュリティ & 信頼性
* GAS側パラメータ検証（難易度、スコア整合性、文字数等）。
* Spreadsheet formula injection対策（先頭 `=`, `+`, `-`, `@` のエスケープ）。
* GAS LockServiceによる同時書き込み排他制御。

---

## 1. プロジェクトの目的
部署内で楽しくプレイしながら自然とタイピングスキル（速度向上、ブラインドタッチ習熟、社内用語習熟）が身につく、仮設足場・積込2DタイピングWebアプリケーションの提供。

## 2. ターゲットユーザー
* 部署内の一般社員・作業スタッフ（PC操作初心者〜中上級者）
* タイピング練習を行いたいスタッフ、社内ランキングを競いたいスタッフ

## 3. ゴールと成功の定義
1. ブラウザ上で軽快に動作し、フォークリフト積込×背景発展演出による爽快なゲーム体験が得られること。
2. 日本語ローマ字入力判定が自然で、ミスなくスムーズにタイピングできること。
3. 練習モードおよび本番モードが正しく機能し、本番スコアがGoogle Spreadsheetへ記録され、月間・歴代ランキングが表示できること。
4. 単語データやゲーム設定（速度、ボーナス、ペナルティ等）が外部定義・設定可能であり、容易に拡張・調整できること。
